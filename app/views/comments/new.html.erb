<%= form_with(model: @comment, url: user_post_comments_path(@user, @post), method: :post,  class: 'm-3') do |form| %>
  <%= form.text_area :text, placeholder: 'Write a comment...', rows: 3, class: 'form-control' %>
  <%= form.submit 'Post Comment', class: 'btn btn-primary mt-2' %>
<% end %>
